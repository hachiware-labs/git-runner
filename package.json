{
  "name": "@research-booster/git-runner",
  "version": "0.0.0",
  "description": "Git checkout and command execution runner over NATS",
  "type": "module",
  "bin": {
    "git-runner": "./bin/git-runner.js"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check bin/git-runner.js && node --check src/cli.js && node --check src/config.js && node --check src/errors.js && node --check src/git.js && node --check src/job-spec.js && node --check src/job-store.js && node --check src/nats-publisher.js"
  },
  "engines": {
    "node": ">=22"
  },
  "files": [
    "bin/",
    "src/",
    "docs/"
  ],
  "license": "UNLICENSED",
  "dependencies": {
    "@nats-io/transport-node": "^3.4.0"
  }
}
